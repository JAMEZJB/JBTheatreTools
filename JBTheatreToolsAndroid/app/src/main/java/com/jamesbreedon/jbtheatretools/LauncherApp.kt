package com.jamesbreedon.jbtheatretools

import android.app.Application

/** Nothing to set up at process start — the launcher's state is built lazily by the repository. */
class LauncherApp : Application()
