# JB Theatre Tools for Android — the version, in one place.
#
# ONE version number across mac / Windows / Android. This launcher's Android build is
# versioned from the same line as JBTheatreTools/Info.plist (CFBundleShortVersionString)
# and JBTheatreToolsWin/JBTheatreTools.csproj (<Version>); when those move, this moves
# with them in the same commit.
#
# versionCode is a monotonic integer — house formula: major*10000 + minor*100 + patch.
versionName=1.31.0
versionCode=13100
